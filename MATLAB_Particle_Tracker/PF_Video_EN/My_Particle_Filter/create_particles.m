function S = create_particles( Npop_particles)
%Npix_resolution,
%rows = randi(Npix_resolution(1), 1, Npop_particles);
%colomns = randi(Npix_resolution(2), 1, Npop_particles);

rows=zeros(1,Npop_particles);
colomns=zeros(1,Npop_particles);
S=[colomns;rows];

end

